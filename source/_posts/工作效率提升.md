---
title: 工作效率提升
typora-root-url: ..
date: 2022-01-16 15:02:05
tags: javascript
---



## 背景

- 需要定时刷新告警列表，及时处理。 告警内容为json原文，需要快速提取关键信息

- 中午午休或吃饭，需要了解告警情况

- 需要水一篇文章





## 整体实现

![image-20220116150734321](/assets/image-20220116150734321.png)



## 平台选择

开始有纠结使用 油猴还是编写插件，但考虑到插件需要额外学习成本，并且公司的Chrome浏览器不能安装插件，于是决定安装Firefox浏览器，使用Tampermonkey编写自定义脚本 实现。 使用油猴做配置也很方便，使用原生JavaScript语言即可。

```javascript
// ==UserScript==
// @name         New Userscript
// @namespace    http://tampermonkey.net/
// @version      0.1
// @description  try to take over the world!
// @author       You
// @match        http://*/*
// @icon         data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==
// @grant        none
// ==/UserScript==

(function() {
    'use strict';

    // Your code here...
})();
```



## Filter页面

### 判断生效代码

```
if (window.location.href.includes('issues/?filter=12512')){
	//your code
}
```



### 定时刷新

使用定时器即可，每30秒触发一次 search 按钮的点击事件

```javascript
setInterval(function() {
  document.getElementsByClassName("aui-button aui-button-primary search-button")[0].click();
}, 30000);
```



### 新增audio元素

```javascript
var pin = document.getElementById("jira-share-trigger")
var gener_audio=document.createElement("audio");
gener_audio.innerHTML='<source src="http://xxxx/xxx.wav" type="audio/mpeg">'
pin.parentNode.prepend(gener_audio)
gener_audio.play() //播放

//同理，新增微信通知或声音通知的 checkbox
```



### 微信通知

微信通知，这里使用了企业微信的 应用发送消息功能，可参考API文档，如何新增应用等此处略过

https://developer.work.weixin.qq.com/document/path/90236

考虑到通用性，我将获取token及发消息使用腾讯云函数 封装成了一个对外接口 (反正免费白嫖腾讯云函数，还比较稳定)

```python
# -*- coding: utf8 -*-
import json, base64, requests
import datetime

class WeChatPush:
    '''企业微信推送'''

    def __init__(self, corpid, agentid, corpsecret):
        # 企业ID
        self.corpid = corpid

        # 企业应用ID
        self.agentid = agentid

        # 应用的凭证密钥
        self.corpsecret = corpsecret

        # access_token
        self.access_token = ''

        # access_token 获取调用接口
        self.token_url = 'https://qyapi.weixin.qq.com/cgi-bin/gettoken?'

        # 应用消息发送接口
        self.send_url = 'https://qyapi.weixin.qq.com/cgi-bin/message/send?access_token='

        # 请求 session
        self.sess = requests.Session()

    def get_token(self):
        '''获取 access_token'''

        res = json.loads(
            self.sess.get(self.token_url + 'corpid=' + self.corpid + '&corpsecret=' + self.corpsecret).text)

        if res['errcode'] != 0:
            return res['errcode']

        self.access_token = res['access_token']

        return 0

    def send(self, content='', user='@all'):
        '''应用消息发送'''


        if self.access_token == '':
            res_get = self.get_token()  # 更新 access_token
            if res_get != 0:
                return res_get

        data = {
            'touser': user,
            'toparty': '@all',
            'totag': '@all',
            'msgtype': 'text',
            'agentid': self.agentid,
            'text': {
                'content': content,
            },
            'safe': 0,
            'enable_id_trans': 0,
            'enable_duplicate_check': 0,
            'duplicate_check_interval': 1800
        }

        res = json.loads(self.sess.post(url=(self.send_url + self.access_token), data=json.dumps(data)).text)

        if res['errcode'] != 0:  # 可能由 access_token 过期造成
            res_get = self.get_token()  # 更新 access_token
            if res_get != 0:
                return res_get

            res = json.loads(self.sess.post(url=(self.send_url + self.access_token), data=json.dumps(data)).text)

            return res['errcode']
        else:
            return 0


def main_handler(event, context):
    para_data = event['queryString']
    path = event['path']
    if path != "/xxxxx":
        return {
            "isBase64Encoded": False,
            "statusCode": 200,
            "headers": {'Content-Type': 'application/json'},
            "body": json.dumps({'you': 'bad boy'})
        }

    push = WeChatPush("xxxx", "xxx", "xxxx")
    res = push.send(content=  (datetime.datetime.now() + datetime.timedelta(hours=8)).strftime('%Y-%m-%d %H:%M:%S') + '\n\n' +para_data.get('xxx'), user="xxxxx")
    if res == 0:
        print('发送消息成功！')
    else:
        print('发送消息失败！错误代码:', res)
    return {
        "isBase64Encoded": False,
        "statusCode": 200,
        "headers": {'Content-Type': 'application/json'},
        "body": json.dumps({'para_data':  para_data.get('text'), 'path': path})
    }
```

这样，页面调用只传入需要发送的消息即可

```javascript
var httpRequest = new XMLHttpRequest();
	httpRequest.open('GET', 'https://service-xxx'+ encodeURIComponent(content), true);
	httpRequest.send();
	httpRequest.onreadystatechange = function () {
	if (httpRequest.readyState == 4 && httpRequest.status == 200) {
		var json = httpRequest.responseText;
		}
	}
```



## Ticket详情页面

获取 raw_log

```javascript
var raw_log = document.getElementById('field-customfield_10219').children[1].children[1].innerText.trim()
raw_log = raw_log.split('\n')
```

其他一些json数据提取并拼接成指定格式，生成按钮，绑定事件，只贴关键代码

```javascript
//解析 json数据， 外面加个try，防止数据格式不对
try {
	var log_obj = JSON.parse(raw_log[x])
} catch(e) {
		continue
}

//生成按钮
var div_par = document.getElementsByClassName("aui-toolbar2-primary")[0]
var gener_desc=document.createElement("div");
gener_desc.setAttribute('class',"aui-buttons pluggable-ops")
gener_desc.innerHTML='<a id="gen_describ" onclick="gen_describ" title="Generate description" class="aui-button toolbar-trigger><span class="icon aui-icon aui-icon-small"></span> <span class="trigger-label">Generate description</span></a>'
div_par.append(gener_desc)


//绑定 click事件，其实生成按钮时加一个 onclick 属性也行
document.getElementById('gen_describ').addEventListener('click', gen_describ, false);

//数组去重后，拼接成字符串
tmp =  Array.from(new Set(tmp))
var des_str = tmp.join("\n")


//打开新的链接
window.open("https://xxxx"+alert_info[y].alert_id)


```

